[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-podcast-workshop-utils"
version = "0.0.1"
description = "Workshop helpers importable as `utils` from notebooks and scripts."
requires-python = ">=3.12"

[tool.setuptools]
packages = ["utils"]
